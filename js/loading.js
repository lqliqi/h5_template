/**
 */
// iframe 引入右侧内容区
window.onload = initPage();
function initPage()
{
    var objLoading = document.getElementById("loading-box");
    if (objLoading != null)
    {
         objLoading.style.display = "none";
    }
}